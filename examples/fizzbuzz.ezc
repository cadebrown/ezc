#!/usr/bin/env ezc

# FizzBuzz 1 to 100
(
    @n
    $n 15 % 0 == ("FizzBuzz") (
        $n 3 % 0 == ("Fizz") (
            $n 5 % 0 == ("Buzz") ($n str) ife
        ) ife
    ) ife
    :
) @fizzbuzz

100 iota (1 +) &! ($fizzbuzz !) each
