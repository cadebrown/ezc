#!/usr/bin/env ezc

# FizzBuzz 1 to 100
(
    @n "" @r
    $n 3 % 0 == ($r "Fizz" | @r) if
    $n 5 % 0 == ($r "Buzz" | @r) if
    $r "" == ($n str @r) if
    $r :
) @fizzbuzz

# fold body: acc elem → swap, drop acc, run fizzbuzz, push dummy acc
100 iota (1 +) &! 0 (~ ; $fizzbuzz ! 0) &/ ;
