os:
  - osx
  - linux

language: c

# needed
sudo: required

compiler:
  - gcc

install:
  - autoreconf -i
  - ./configure
  - make clean all
  - sudo make install

notifications:
  email:
    recipients:
      - info@chemicaldevelopment.us
    on_success: change
    on_failure: change
