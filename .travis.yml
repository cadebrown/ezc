os:
  - osx
  - linux

language: c

# needed
sudo: required

install:
  - make build build-utils
  # this will fail and say the build has cancelled
  - make check
  - make bundle package
#  - make build

script:
  - make upload
  - make upload-package
#  - make check-global

notifications:
  email:
    recipients:
      - info@chemicaldevelopment.us
    on_success: change
    on_failure: change
